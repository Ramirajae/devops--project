apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
  namespace: devops-app

spec:
  selector:
    app: devops-api

  type: NodePort        # On garde NodePort, c'est Minikube qui gère l'accès
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080